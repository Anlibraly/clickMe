let g = require('../../common/const');
let msg = require('../../common/msg');

let intoRoom = (req, socket, io) => {
    
    if (req.roomId) {
        
    }
    
};

export default {
    intoRoom
};
